#include <assert.h>
#include <iostream>

// #include "spacemanager.h"

using namespace std;

int main()
{
    // SpaceManager sman;

    // sman.open("../data/test.db/idx000");

    // auto chuck = sman.alloc(10);
    // assert(chuck.first == 0 && chuck.second == 0);

    // sman.free(0, 10);

    // sman.close();

    cout << "Correctly ending" << endl;
}